<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Get Started - TIXIFY</title>
  <script src="https://cdn.tailwindcss.com"></script>

  {# ✅ Toastify CSS + JS #}
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
  <script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
</head>
<body class="flex flex-col items-center justify-center min-h-screen bg-gradient-to-r from-indigo-600 to-blue-600 text-black font-sans">

  <h1 class="text-white text-4xl font-extrabold mb-8">TIXIFY</h1>

  <form method="POST" action="getStarted.php" class="bg-white rounded-lg shadow-md p-8 w-full max-w-md flex flex-col gap-3">
    <h2 class="text-2xl font-bold text-center mb-2">Sign Up</h2>
    <p class="text-gray-600 text-center mb-4">Create your TIXIFY account</p>

    <label for="fname" class="font-semibold">First Name</label>
    <input type="text" id="fname" name="fname" class="border-2 border-gray-300 rounded p-2 w-full" required>

    <label for="lname" class="font-semibold">Last Name</label>
    <input type="text" id="lname" name="lname" class="border-2 border-gray-300 rounded p-2 w-full" required>

    <label for="username" class="font-semibold">Username</label>
    <input type="text" id="username" name="username" class="border-2 border-gray-300 rounded p-2 w-full" required>

    <label for="email" class="font-semibold">Email</label>
    <input type="email" id="email" name="email" class="border-2 border-gray-300 rounded p-2 w-full" required>

    <label for="password" class="font-semibold">Password</label>
    <input type="password" id="password" name="password" class="border-2 border-gray-300 rounded p-2 w-full" required>

    <button type="submit" class="bg-indigo-600 hover:bg-indigo-800 text-white font-bold py-2 px-4 rounded mt-4 transition-transform transform hover:scale-105">
      Sign Up
    </button>
  </form>

  {# ✅ Toast messages #}
  {% if success %}
  <script>
    Toastify({
      text: "{{ success }}",
      duration: 4000,
      gravity: "top",
      position: "right",
      backgroundColor: "linear-gradient(to right, #16a34a, #22c55e)"
    }).showToast();
  </script>
  {% endif %}

  {% if error %}
  <script>
    Toastify({
      text: "{{ error }}",
      duration: 4000,
      gravity: "top",
      position: "right",
      backgroundColor: "linear-gradient(to right, #dc2626, #ef4444)"
    }).showToast();
  </script>
  {% endif %}
</body>
</html>
